# ===========================================
# SUPABASE CONFIGURATION (Primary Database)
# ===========================================

# Get these from your Supabase project settings
# https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Service role key for server-side operations (bypasses RLS)
# KEEP THIS SECRET! Only use in API routes, never expose to client
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# ===========================================
# NEXTAUTH CONFIGURATION
# ===========================================

# Generate a secure random secret with: openssl rand -base64 32
# IMPORTANT: Use a different secret for production!
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"

# Your application URL
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# ===========================================
# YOUTUBE API CONFIGURATION
# ===========================================

# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable YouTube Data API v3 for your project
# Note: Free tier has quota limits (10,000 units/day)
YOUTUBE_API_KEY="your-youtube-api-key"

# Optional: YouTube Music cookies for ytmusic-api
# Use for region-specific results or account-specific playlists
# YTMUSIC_COOKIES="cookie-string"

# ===========================================
# OPTIONAL CONFIGURATIONS
# ===========================================

# Database URL (if using Prisma instead of Supabase)
# DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
